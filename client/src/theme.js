import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1B998B',
    },
    secondary: {
      main: '#F8F1FF',
    },
    background: {
      default: '#DECDF5',
    },
    text: {
      primary: '#1C1D21',
    },
  },
});

export default theme;